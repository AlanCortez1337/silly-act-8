#ifndef UPDATE_EDGE_WEIGHT_HPP
#define UPDATE_EDGE_WEIGHT_HPP
template <typename T>
void alGraph<T>::UpdateEdgeWeight(std::size_t pointA, std::size_t pointB, std::size_t newEdgeWeight)
    {
        // ⭐TODO: given two points, go in the matrix and update the value at that spot
    }

#endif // UPDATE_EDGE_WEIGHT_HPP
