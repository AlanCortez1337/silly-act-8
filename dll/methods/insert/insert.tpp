#ifndef INSERT_TPP
#define INSERT_TPP

template <typename T>
void DLL<T>::insert(T value, T target) {
    // ⭐TODO: Find the node with the same value as target. 
    // If you do find it then insert a new node with "value" as the data
}

#endif // INSERT_TPP
