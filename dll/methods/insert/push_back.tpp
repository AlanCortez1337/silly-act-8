#ifndef PUSH_BACK_TPP
#define PUSH_BACK_TPP

template <typename T>
void DLL<T>::push_back(T value) {
    // ⭐TODO: create a new node with "value" as the data and insert it to the back of the linked list
}

#endif // PUSH_BACK_TPP
